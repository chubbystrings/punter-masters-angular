import { CoreAnimationDirectiveDirective } from './core-animation-directive.directive';

describe('CoreAnimationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreAnimationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
